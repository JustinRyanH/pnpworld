<nav class="nav" data-controller="navbar">
  <div class="nav-container">
    <%= header %>
    <!--    TODO: Extract this as a Navbar Component that is tested-->
    <button <%= tag.attributes(
                  {
                    type: 'button',
                    class: 'nav-mobile-btn',
                    'aria-controls': 'navbar-default',
                    'aria-expanded': 'false',
                    data: {
                      'navbar-target': 'openButton',
                      action: "click->navbar#toggle"
                    }
                  }) %>>
      <svg class="w-6 h-6" aria-hidden="true" fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg">
        <path fill-rule="evenodd" d="M3 5a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM3 10a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1zM3 15a1 1 0 011-1h12a1 1 0 110 2H4a1 1 0 01-1-1z" clip-rule="evenodd"></path>
      </svg>
    </button>
    <div class="hidden w-full md:block md:w-auto" id="navbar-default">
      <ul class="flex flex-col p-4 mt-4 bg-gray-50 rounded-lg border border-gray-100 md:flex-row md:space-x-4 md:mt-0 md:text-sm md:font-medium md:border-0 md:bg-white dark:bg-gray-800 md:dark:bg-gray-900 dark:border-zinc-500">
        <% links.each do |link| %>
          <%= link %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>